name: rclrs_iron

on:
  push:
    branches:
      - rclrs
  pull_request:
    branches:
      - rclrs
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  tests_iron:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: bash -lc "./integration/scripts/run-ros-tests-docker.sh 'derive,nalgebra,rayon,serde' docker/Dockerfile_rclrs_iron copy"
